import './SortSelector.css';

const SortSelector = (props) => {
  return (
    <div id="sort-selector">
      {"Sort by: "}
      <select>
        <option></option>
      </select>
      <select>
        <option></option>
      </select>
    </div>
  );
};

export default SortSelector;
