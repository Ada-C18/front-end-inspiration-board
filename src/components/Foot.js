import React, { useState } from "react";
import PropTypes from "prop-types";

const Foot = (props) => {
    return (
        <h6>This is a demo! Please be gentle! Click here to delete all boards and cards!</h6>
    );
};